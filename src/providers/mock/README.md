# Mock Providers

Mock implementations for market, news, and generative features will live here when their domain contracts are introduced. Stage 1 deliberately contains no sample market or AI response data.
